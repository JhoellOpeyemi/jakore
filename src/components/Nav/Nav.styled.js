import styled from "styled-components";

export const StyledNav = styled.nav`
  position: absolute;
  top: 2.5rem;
  left: 0;
  width: 100%;
  text-align: center;

  a:not(:last-child) {
    margin-right: 3.5rem;
  }
`;
